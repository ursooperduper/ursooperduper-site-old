\---
layout: base
\---

%h2.section-heading<= "Posts by Tag"

{% include tags.html %}

%hr

%h2.section-heading {{ page.tag }}

.tag-list
  %ul
    {% for post in site.tags[page.tag] %}
    %li
      %a{href: "{{ post.url }}"}
        {{ post.title }}

      %span.post-meta<
        {{ post.date | date: "%b %-d, %Y" }}
    {% endfor %}
