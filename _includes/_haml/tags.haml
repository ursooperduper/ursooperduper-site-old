.tags
  {% assign sortedTags = site.tags | sort %}
  {% for t in sortedTags %}
  {% assign tag = t | first %}
  .tag<
    %a{href: "/tags/{{ tag | downcase | replace:' ','-'}}" }<
      {{ tag }}
  {% endfor %}
